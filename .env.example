# MongoDB Atlas connection string (from Atlas: Connect > Drivers)
# Create a free M0 cluster, add a DB user, whitelist 0.0.0.0/0, then copy this.
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net

# Database name (optional, defaults to yuvotsav2026)
MONGODB_DB=yuvotsav2026
